'use client'

// Branch Admin Inventory - Uses same component as admin but filtered by branch
import InventoryPage from '@/app/admin/inventory/page'

export default function BranchAdminInventoryPage() {
  return <InventoryPage />
}
