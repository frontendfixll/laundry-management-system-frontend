'use client'

// Branch Admin Tickets - Uses same component as admin but filtered by branch
import TicketsPage from '@/app/admin/tickets/page'

export default function BranchAdminTicketsPage() {
  return <TicketsPage />
}
