'use client'

// Branch Admin Orders - Uses same component as admin but filtered by branch
import OrdersPage from '@/app/admin/orders/page'

export default function BranchAdminOrdersPage() {
  return <OrdersPage />
}
